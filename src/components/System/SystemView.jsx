export default function SystemView({ connection }) {
  return (
    <div style={{ padding: '2rem', color: 'var(--text)' }}>
      <h2>{connection.name}</h2>
      <p>En construcción — CI-DS System View</p>
    </div>
  )
}
